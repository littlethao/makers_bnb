<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
<body>

  <%= message %>

  <h2>Requests you've made:</h2>

  <ul>

    <% for(var i=0; i<requests.length; i++) { %>
      <%= requests[i].spaces.title %> - <%= requests[i].spaces.description %> - <%= requests[i].spaces.date %> - <%= requests[i].spaces.price %> - <%= requests[i].users.email %> 
      <br>
      <% } %>
  </ul>


</body>
